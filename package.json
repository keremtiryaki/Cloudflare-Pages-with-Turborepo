{
  "name": "turborepo-cf",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "deploy": "cd apps/api-hono && pnpm run deploy && cd ../api-hono-2 && pnpm run deploy && cd ../web-astro && pnpm run deploy",
    "gen-sql": "cd packages/lib && pnpm gen-sql",
    "local-migrate": "cd packages/lib && pnpm local-migrate",
    "remote-create-db": "cd packages/lib && pnpm remote-create-db",
    "remote-migrate": "cd packages/lib && pnpm remote-migrate"
  },
  "devDependencies": {
    "turbo": "^2.0.12",
    "typescript": "^5.5.2",
    "npm-run-all": "^4.1.5",
    "wait-on": "^7.2.0"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
