---
import { users } from 'lib/db/schema/users'
import { initializeDrizzle } from 'lib/db/initializeDrizzle'
const DATABASE = Astro.locals.runtime.env.D1_DB;
const DRIZZLE = initializeDrizzle(DATABASE)

const id = Math.random().toString(36).substring(7);
await DRIZZLE.insert(users).values({id});
---

production:{import.meta.env.PROD}
<br>
✅user with id {id}